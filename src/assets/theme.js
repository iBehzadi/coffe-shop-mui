import { createTheme } from "@mui/material";
const theme = createTheme({
    palette: {
        primary: {
            main: "#6f4e37",
        },
        secondary: {
            main: "#f5f5dc",
        },
    },
});
export default theme;